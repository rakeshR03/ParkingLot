package strategies.dynamic;

import models.Ticket;
import strategies.FeesStrategy;

public class MediumVehicleDynamicFeesStrategy implements FeesStrategy {
    @Override
    public Integer calculateFees(Ticket ticket) {
        return null;
    }
}
