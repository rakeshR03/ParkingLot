package strategies.timebased;

import models.Ticket;
import strategies.FeesStrategy;

public class MediumVehicleFeesStrategy implements FeesStrategy {
    @Override
    public Integer calculateFees(Ticket ticket) {
        return null;
    }
}
